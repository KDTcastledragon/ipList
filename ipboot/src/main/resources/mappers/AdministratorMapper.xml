<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iplist.ipboot.mapper.AdministratorMapper">
	<select id="selectById" resultType="com.iplist.ipboot.domain.AdministratorDTO">
        SELECT * FROM administrator where admin_id = #{adminId};
    </select>
</mapper>